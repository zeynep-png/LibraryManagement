<nav class="navbar navbar-expand-lg fixed-top" style="background-color: #2E2E2E; border-radius: 50px;">
    <div class="container-fluid">
        <a class="navbar-brand text-white" href="/">Library Management System</a>
        <a class="navbar-brand text-white" href="/Home/About">About</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active text-white" aria-current="page" href="/Book/BookList">Book List</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active text-white" aria-current="page" href="/Author/List">Author List</a>
                </li>
            </ul>

            <!-- Giriş yapılmışsa gösterilecek -->
            @if (User.Identity.IsAuthenticated)
            {
                <div class="d-flex align-items-center">
                    
                    <form method="post" asp-controller="Auth" asp-action="Logout">
                        <button type="submit" class="btn btn-outline-light rounded-pill">Logout</button>
                    </form>
                </div>
            }
            else
            {
                <!-- Giriş yapılmamışsa Login ve Sign Up butonları gösterilecek -->
                <div class="d-flex">
                    <a class="btn btn-outline-light me-2 rounded-pill" href="/Auth/Login">Login</a>
                    <a class="btn btn-outline-light rounded-pill" href="/Auth/SignUp">Sign Up</a>
                </div>
            }
        </div>
    </div>
</nav>


<!-- Optional custom CSS for further styling -->
<style>
    .navbar {
        background-color: #2E2E2E; /* Dark grey (like ChatGPT's dark mode) */
        border-radius: 50px; /* Rounded corners */
        position: sticky; /* Keep navbar fixed */
        top: 0; /* Stick to the top */
        z-index: 1000; /* Ensure it stays above other elements */
        margin: 10px; /* Optional: adds a little spacing around navbar */
    }

        .navbar .navbar-brand, .navbar .nav-link {
            color: white; /* Text color */
        }

    .btn-outline-light {
        border-color: white; /* White outline for buttons */
        color: white; /* White text for buttons */
    }

        .btn-outline-light:hover {
            background-color: white;
            color: #2E2E2E; /* Dark grey text on hover */
        }

    .rounded-pill {
        border-radius: 50px; /* Rounded corners */
    }
</style>
